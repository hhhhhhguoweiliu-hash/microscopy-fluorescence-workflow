# 显微图像红色荧光定量工作流与确认边界

本文档总结了一套从 Leica LIF/TIFF 图像整理、ImageJ 红色通道平均荧光强度计算，到 Prism 数据表和 PowerPoint 图像汇总的可复用流程。

核心原则只有一条：**数值、图片编号、图片来源和最终展示必须始终能够一一对应。**

## 一、完整工作流程

### 1. 原始文件检查

- 清点 LIF、TIFF、MetaData、CSV、Excel、ImageJ macro 和 PPT 文件。
- 确认图像编号范围、通道后缀、ICC/非ICC版本及放大倍数。
- 原始采集文件保持只读，处理结果放入新的输出目录。

### 2. 图像分组与命名

- 根据用户确认的编号边界添加组名前缀。
- 保留原始 Image 编号，避免后续无法对应定量数据。
- 将10倍镜、20倍镜及ICC/原始图分别整理。
- 批量删除、移动或重命名前，必须明确边界是否包含端点。

### 3. 荧光阈值与导出

- 区分显微镜采集参数和导出显示范围。
- 同一比较中的曝光、增益、通道和导出范围必须一致。
- 阈值会改变图像显示，若对映射后的RGB图计算，还可能改变测量值。
- 阈值选择属于实验判断，不能为了让正常组“完全无荧光”而机械调整。

### 4. ImageJ批量测量

- 打开ICC图。
- Split Channels。
- 选择经确认的红色通道。
- 只计算 Mean fluorescence intensity。
- 保留简化后的图片名称。
- 不自动加入 rolling ball、阈值、分割或其他参数，除非用户明确要求。

旧版 ImageJ 无法处理中文路径时，应使用英文路径或目录联接，输出后再移动到实验目录。

### 5. Excel与Prism整理

推荐保留三个层次：

1. 全部数据；
2. 速查表（组别、Image名称、Image编号、Mean、来源单元格）；
3. Prism格式数据。

如果用黄色底纹表示剔除：

- 先确认黄色的含义；
- 从指定sheet读取；
- 黄色剔除，未填色保留；
- 按完整数据重新匹配Image编号；
- 更新速查表和Prism表；
- 汇报每组数量与最终编号。

### 6. Prism统计

- Column格式：每组一列，重复值向下排列。
- Grouped格式：适合同时比较氧条件与处理方式等两个因素。
- ANOVA选择需要结合实验设计，而不是只看表格长相。

### 7. PPT图像汇总

- 速查表决定使用哪些Image编号。
- 用户确认的文件夹决定图片来源。
- 每个Image匹配明场、红色通道和合并图。
- 图片保持原始比例，不拉伸。
- 比例尺不得裁切。
- 用户提供样例时，样例决定版式，但不能替代速查表中的编号。
- 批量制作前先确认一页样例；若用户已编辑样例，则以编辑后的样例为版式标准。

## 二、普遍适用、可以直接执行的工作

在规则已经明确且操作不破坏原始数据时，可以直接进行：

- 扫描文件夹并统计文件数量；
- 检查三通道图片是否配套；
- 简化名称但保留Image编号；
- 生成非破坏性的重命名/移动清单；
- 按确认的通道和指标生成ImageJ macro；
- 将已确认的数据整理为Prism Column或Grouped格式；
- 按已确认的黄色标记更新速查表；
- 根据速查表和指定图片目录生成PPT；
- 检查Excel公式、数据数量、PPT缺图、裁切和变形。

## 三、必须让用户确认的节点

- 哪个文件夹是当前权威图片来源；
- 使用ICC图还是非ICC图；
- 红色荧光对应哪个通道；
- 编号边界是否包含端点；
- 是否删除、移动或覆盖文件；
- 黄色底纹是否表示剔除；
- 哪个sheet是最终筛选依据；
- 新导出版本是否替代旧版本；
- PPT样例是否可以批量套用；
- 是否覆盖已有Excel或PPT。

确认时必须写出具体路径、编号和边界示例，不能只问“这样可以吗”。

## 四、需要与用户共同讨论的工作

这些事项影响实验结论，不能由自动化流程替用户决定：

- 荧光阈值或显示范围；
- 是否测量原始强度还是显示映射后的RGB值；
- 缺氧/有氧理论上哪个组更高；
- 离群值是否剔除；
- 是否按细胞数、面积或背景归一化；
- 10倍镜和20倍镜是否能够合并分析；
- 不同拍摄批次是否能够合并；
- 使用单因素、双因素还是重复测量方差分析；
- 数据是生物学重复还是技术重复；
- 是否需要配对分析。

自动化工具应提供数据、图像和统计依据，但最终判断应由实验设计与用户决定。

## 五、交付前检查清单

- [ ] 来源文件夹路径准确；
- [ ] ICC/非ICC版本准确；
- [ ] 放大倍数准确；
- [ ] 分组边界准确；
- [ ] 每组数据数量准确；
- [ ] 速查表的Image编号与完整数据一致；
- [ ] Prism数据与速查表一致；
- [ ] PPT图片编号与速查表一致；
- [ ] 每个Image的三通道齐全；
- [ ] 比例尺完整；
- [ ] 图片未拉伸、未裁切；
- [ ] 新旧阈值版本没有混用；
- [ ] 所有输出经过渲染检查；
- [ ] 未解决的科学问题已明确列出。
